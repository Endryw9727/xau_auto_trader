apiVersion: v1
kind: Service
metadata:
  name: xau-dashboard-service
  namespace: xau-trader
spec:
  selector:
    app: xau-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8501
  type: LoadBalancer

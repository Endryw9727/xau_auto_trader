# Future Execution Layer

This directory is a disabled placeholder for a possible future execution layer.
It does not contain broker connectors, order functions, API clients, API keys,
or live trading code.

Live trading remains disabled. `LIVE_MODE` must stay false and `allow_live`
must stay false. Any future execution work requires explicit manual approval,
demo-first validation, separate tests, and a safety audit.
